services:
  - type: web
    name: kazakhoil-api
    runtime: python
    rootDir: apps/api
    buildCommand: pip install -e .
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: ENERGOPULSE_APP_ENV
        value: production
      - key: ENERGOPULSE_DATABASE_URL
        fromDatabase:
          name: kazakhoil-postgres
          property: connectionString

  - type: worker
    name: kazakhoil-worker
    runtime: python
    rootDir: apps/worker
    buildCommand: pip install -e .
    startCommand: worker
    envVars:
      - key: ENERGOPULSE_APP_ENV
        value: production
      - key: ENERGOPULSE_DATABASE_URL
        fromDatabase:
          name: kazakhoil-postgres
          property: connectionString

  - type: web
    name: kazakhoil-web
    runtime: static
    rootDir: apps/web
    buildCommand: corepack pnpm install --no-frozen-lockfile && corepack pnpm --filter web run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://kazakhoil-api.onrender.com

databases:
  - name: kazakhoil-postgres
    databaseName: energopulse
    user: energopulse