# Release plan — доработки аналитики Kazakhoil

Статус релиза: **готов к визуальной приёмке**

Источник требований: `Казахойл - Доработки.docx`.

## R1. Компоновка существующих дэшбордов

- [x] Перенести «Полноту дневных данных» из «Энергобаланса» в «Пики и аномалии».
- [x] Удалить визуализацию «Линии 35 кВ» из «Энергобаланса».
- [ ] Проверить desktop/mobile-компоновку после изменения сетки.

## R2. Семантика внешних потребителей

- [x] Нормализовать номер прибора учёта и использовать его как стабильную часть ID.
- [x] Извлекать компанию из наименования потребителя.
- [x] Объединять варианты `GasProces.Comp`, `GasProcsComp`, `GasProsComp`.
- [x] Учитывать отдельный сводный блок «Каспий нефть» и «КазТрансОйл».
- [x] Возвращать нераспознанные связи явно, без потери объёма.
- [x] Проверять схождение детализации с официальным итогом внешнего потребления.

## R3. Потребители и погодные регионы

- [x] Назначать новым потребителям регион «Актюбинская область» по умолчанию.
- [x] Показывать компанию и площадку отдельными полями.
- [x] Сохранять пользовательское переопределение региона.

## R4. Внешнее потребление по подстанциям

- [x] Построить справочник `счётчик → компания → площадка → подстанция`.
- [x] Добавить API-разбивку внешнего потребления по подстанциям.
- [x] Перевести соответствующий график на подстанции Казахойла.
- [x] Добавить категорию «Требует уточнения» для неоднозначных строк.

## R5. Новые дэшборды

- [x] Добавить API «Тех. баланс».
- [x] Добавить экран «Тех. баланс» с таблицей объектов и распределением расхода.
- [x] Добавить API «Ежедневное потребление».
- [x] Добавить экран «Ежедневное потребление» с рейтингом счётчиков и подстанций.
- [x] Добавить страницы в навигацию и пустые/ошибочные состояния.

## R6. Прогнозирование

- [x] Разделить прогноз «Казахойл» и «Внешние потребители».
- [x] Добавить прогнозные показатели по подстанциям.
- [x] Проверить, что суммы сегментов сходятся с общим прогнозом.

## R7. Контроль качества релиза

- [x] Добавить unit-тесты нормализации компаний, счётчиков и подстанций.
- [x] Добавить API-тесты новых разрезов.
- [x] Выполнить backend test suite.
- [x] Выполнить production-сборку frontend.
- [x] Обновить пользовательскую документацию и карту проекта.

## Результаты проверки

- Backend: `10 passed`.
- Frontend: production-сборка Vite выполнена без ошибок.
- Внешнее потребление: официальный итог и сумма детализации равны
  `2 342 306,25 кВт·ч`, расхождение — `0`.
- Прогноз: сумма показателей по подстанциям совпадает с прогнозом внешнего
  потребления.
- Визуальная проверка desktop/mobile остаётся открытой: встроенный браузер Codex
  не получил доступ к локальному dev-серверу. Код адаптивных сеток проверен
  статически, production-сборка проходит.

## Критерий выпуска

Функциональная часть релиза готова. Для окончательного выпуска остаётся
визуальная приёмка desktop/mobile-компоновки; остальные критерии выполнены.
